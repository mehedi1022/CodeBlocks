#include <bits/stdc++.h>
using namespace std;

int main(){
    long long x, y, z, p, q;
    cin >> x >> y >> z >> p;
    q = (x  * y) - (z * p);

    cout << "Difference" <<" "<< "=" << " " << q << endl;
    return 0;
}